# Port de l'interface (Pilot l'injecte à la création). Next lit la variable PORT.
PORT=3000

# Secret de signature de session (généré par post-init.sh). NE PAS committer .env
AUTH_SECRET=change-me-generate-a-long-random-string

# Compte admin de CETTE interface (un par interface).
# ADMIN_PASSWORD_HASH = bcrypt du mot de passe, généré par `npm run seed:admin`.
ADMIN_EMAIL=admin@example.local
ADMIN_PASSWORD_HASH=

# Console de création — clé API Anthropic. Utilisée UNIQUEMENT par la route
# serveur app/api/claude/route.ts. Ne jamais committer la vraie clé : la mettre
# dans .env.local (couvert par .gitignore). Jamais exposée au bundle client.
ANTHROPIC_API_KEY=

# Mode hybride (fournisseur local, gratuit) — optionnel : ces valeurs par
# défaut conviennent si Ollama et le conteneur SearXNG tournent en local.
# OLLAMA_URL=http://localhost:11434
# SEARXNG_URL=http://localhost:8888
